import { RevealAnimation } from "../utils/RevealAnimation";

export const Quoute = () => {
  return (
    <RevealAnimation>
      <blockquote className="container mx-auto text-center text-4xl font-extrabold italic text-slate-600 font-quote">
        "I've always had big dreams of changing the world, but they keep telling
        me I can't modify the source code!"
      </blockquote>
    </RevealAnimation>
  );
};
